import { sdk } from '../../sdk'

/**
 * Here you define the volumes and paths of any dependencies needed by this service
 *
 * See Hello Moon for an example
 */
export const dependencyMounts = sdk.setupDependencyMounts()
